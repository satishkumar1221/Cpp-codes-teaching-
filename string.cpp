#include<iostream>
#include<string>
#include<maps>
#include<memory>
using namespace std;
int main()
{
  string str;
  cin>>x;
  int p;
  p = str.length();
  char *c = new char[p];
  // declare a vector or a stack//
  //implementing this with a stack will be a better idea//
  stack<char> pnew;
  stack<char>qnew;  

  for(int i=0 ; i<str.length();i++)
  {
    if(str[i]==str[i+1])
    {

    }
    //update the length each time
  }
  return 0;
}
